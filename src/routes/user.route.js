import express from "express";

import { authJWT } from "../middlewares/passport.middleware.js";

const router = express.Router();

// router.get("/profile", authJWT);


// router.put("/profile", authJWT);


export default router;
